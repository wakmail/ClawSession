# TODO

- [ ] Send responses somewhere (Slack, email, macOS notification, etc.) — maybe
- [ ] Refactor to asyncio-based approach (Approach C) — concurrent message sends, non-blocking loop (for more advanced use)
- [ ] Add `--model` flag to `sender.py` for standalone use (with short aliases like `haiku`, `sonnet`, `opus`)
